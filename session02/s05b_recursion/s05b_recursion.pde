void setup() {
  // sum the numbers from 1 to 100
  
  // sum the even numbers from 1 to 100
  
  // compute the product 3*5*7*... 11
  
  // draw a grid
  
  // draw a bullseye
  
  // draw concentric rectangles alternating color
  
  
  
  int sum = 0;
  for (int i = a; i <= b; i += 2)
    sum += i;
    
  int prod = 1;
  for (int i = a; i <= b; i += 2)
    sum *= i;